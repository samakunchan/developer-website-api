services:
  api:
    build:
      context: .
    restart: always
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB']
      interval: 10s
      timeout: 5s
      retries: 5